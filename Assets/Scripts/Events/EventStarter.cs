using UnityEngine;

public class EventStarter : MonoBehaviour
{

    // This script will be inherited by other scripts to have their event run when GameTimer runs runEvent

    public void runEvent()
    {
        return;
    }
}
